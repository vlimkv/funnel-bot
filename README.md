# Telegram Bot для Прогрева Аудитории 🤖

Бот для автоматического прогрева пользователей с системой отложенных сообщений, админ-панелью и управлением материалами.

## 🎯 Основные возможности

### Для пользователей:
- 🎁 Получение бесплатного комплекса (PDF или ссылка)
- ✉️ Возможность оставить контакты
- 📱 Ссылки на Instagram и сайт/курс
- 🔥 Автоматический прогрев с отложенными сообщениями

### Для администраторов:
- 📊 Полная статистика бота
- 🔗 Управление ссылками и материалами
- 👥 Просмотр списка пользователей
- 📢 Массовая рассылка
- 🗑 Управление очередью сообщений

## 📋 Что изменилось

### ✅ Убрано:
- ❌ Кнопка "Пауза прогрева" (теперь прогрев всегда активен)
- ❌ Команды "Пауза" и "Старт"

### ✨ Улучшено:
- ✅ Админ-панель с возможностью загрузки PDF-файлов
- ✅ Сохранение ссылок в базе данных
- ✅ Возможность отправки файлов напрямую через бота
- ✅ Улучшенная обработка материалов

## 🚀 Быстрый старт

### 1. Установка Docker (если не установлен)

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install docker.io docker-compose

# macOS
brew install docker docker-compose
```

### 2. Клонирование и настройка

```bash
# Клонировать репозиторий
git clone <your-repo-url>
cd telegram-bot

# Создать .env файл
cp .env.example .env

# Отредактировать .env и добавить BOT_TOKEN
nano .env
```

### 3. Запуск бота

```bash
# Быстрый старт (автоматически запустит Docker)
chmod +x quick-start.sh
./quick-start.sh

# Или вручную
docker-compose up -d

# Просмотр логов
docker-compose logs -f telegram-bot
```

## ⚙️ Конфигурация

### Переменные окружения (.env)

```env
# Основные настройки
BOT_TOKEN=your_bot_token_here           # Токен от @BotFather
DATABASE_URL=postgresql://...            # URL PostgreSQL базы

# Ссылки (настраиваются через админ-панель)
FREEBIE_URL=https://...                 # Бесплатный комплекс
NEXT_MATERIAL_URL=https://...           # Следующий материал
COURSE_URL=https://...                  # Курс
INSTAGRAM_URL=https://instagram.com/... # Instagram профиль
BASE_WEBAPP_URL=https://...             # Сайт

# Дополнительно
TIMEZONE=Asia/Almaty                    # Часовой пояс
TEST_MODE=0                             # 1 = тест (минуты), 0 = продакшн (дни)
```

## 👨‍💼 Админ-панель

### Доступ к админ-панели

1. Добавьте свой Telegram ID в `src/routers/admin.py`:
```python
ADMIN_IDS = [7042937865, YOUR_ID_HERE]
```

2. Отправьте команду `/admin` в боте

### Функции админ-панели

#### 📊 Статистика
- Общее количество пользователей
- Новые за 24 часа / неделю
- Пользователи с email
- Активные / на паузе
- Количество сообщений в очереди
- Отправленные сообщения
- Рефералы

#### 🔗 Управление ссылками
Вы можете установить:
- **Бесплатный комплекс**: PDF-файл или ссылку
- **Следующий материал**: PDF-файл или ссылку
- **Курс**: ссылку на курс
- **Instagram**: ссылку на профиль

**Как загрузить PDF:**
1. Нажмите на нужный материал в админ-панели
2. Отправьте PDF-файл боту
3. Бот сохранит file_id и будет отправлять этот файл пользователям

**Как установить ссылку:**
1. Нажмите на нужный материал в админ-панели
2. Отправьте текстовое сообщение со ссылкой
3. Ссылка будет сохранена

#### 👥 Список пользователей
- Просмотр последних 20 пользователей
- Имя, email, статус паузы
- Дата регистрации

#### 📢 Рассылка
- Отправка сообщения всем активным пользователям
- Статистика доставки

#### 🗑 Очистка очереди
- Удаление всех запланированных сообщений
- Полезно при тестировании

## 🔄 Логика прогрева

### Автоматические сообщения

**В продакшн-режиме (TEST_MODE=0):**
- День 1: Сообщение с вопросом об ощущениях
- День 2: Материал о важности укрепления тазового дна

**В тест-режиме (TEST_MODE=1):**
- Через 2 минуты: Первое сообщение
- Через 5 минут: Второе сообщение

### Команды пользователя

- **Стоп / Отписка**: Остановить все сообщения
- **Комплекс**: Получить бесплатный комплекс повторно
- **Контакт**: Оставить email и имя

## 📁 Структура проекта

```
telegram-bot/
├── src/
│   ├── routers/
│   │   ├── __init__.py      # Регистрация роутеров
│   │   ├── user.py          # Обработчики пользователя
│   │   └── admin.py         # Админ-панель
│   ├── config.py            # Конфигурация
│   ├── db.py                # Работа с БД
│   ├── keyboards.py         # Клавиатуры
│   ├── scheduler.py         # Планировщик
│   ├── texts.py             # Тексты сообщений
│   ├── utils.py             # Утилиты
│   └── main.py              # Точка входа
├── .env                     # Переменные окружения
├── .env.example             # Пример .env
├── docker-compose.yml       # Docker конфигурация
├── Dockerfile               # Docker образ
├── init_db.sql              # SQL схема
├── requirements.txt         # Python зависимости
├── quick-start.sh           # Скрипт быстрого старта
└── README.md                # Эта инструкция
```

## 🛠 Разработка

### Локальный запуск без Docker

```bash
# Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt

# Запустить PostgreSQL локально или использовать внешнюю БД

# Запустить бота
python -m src.main
```

### Просмотр логов

```bash
# Все логи
docker-compose logs -f

# Только бот
docker-compose logs -f telegram-bot

# Только база данных
docker-compose logs -f postgres
```

### Остановка и перезапуск

```bash
# Остановить
docker-compose down

# Остановить и удалить данные
docker-compose down -v

# Перезапустить
docker-compose restart

# Пересобрать и запустить
docker-compose up -d --build
```

## 🐛 Решение проблем

### Бот не запускается

1. Проверьте логи: `docker-compose logs telegram-bot`
2. Убедитесь, что BOT_TOKEN правильный
3. Проверьте соединение с БД

### База данных не подключается

1. Проверьте, что PostgreSQL запущен: `docker-compose ps`
2. Проверьте DATABASE_URL в .env
3. Пересоздайте контейнеры: `docker-compose down -v && docker-compose up -d`

### Сообщения не отправляются

1. Проверьте, что планировщик работает (в логах должно быть "Scheduler started")
2. Проверьте очередь сообщений через админ-панель
3. Убедитесь, что TEST_MODE установлен правильно

## 📝 TODO / Планы

- [ ] Веб-интерфейс для админки
- [ ] Аналитика конверсий
- [ ] A/B тестирование сообщений
- [ ] Сегментация пользователей
- [ ] Интеграция с CRM
- [ ] Экспорт данных

## 📄 Лицензия

MIT License

## 🤝 Поддержка

Если возникли вопросы или проблемы, создайте Issue в репозитории.

---

Сделано с ❤️ для эффективного прогрева аудитории